class Box {
  let value;

  fn init(value) {
    this.value = value;
  }
  

  fn get() => this.value;
}

let box = Box("Hello");

println(box.get()); // out: Hello

box.value = "World";

println(box.get()); // out: World
