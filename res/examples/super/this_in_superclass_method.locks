class Base {
  fn init(a) {
    this.a = a;
  }
}

class Derived extends Base {
  fn init(a, b) {
    super.init(a);
    this.b = b;
  }
}

let derived = Derived("a", "b");
print derived.a; // out: a
print derived.b; // out: b
