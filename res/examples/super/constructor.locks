class Base {
  fn init(a, b) {
    print "Base.init(" + a + ", " + b + ")";
  }
}

class Derived extends Base {
  fn init() {
    print "Derived.init()";
    super.init("a", "b");
  }
}

Derived();
// out: Derived.init()
// out: Base.init(a, b)
