class Record {
  let key = "value";
}

let the_key = "key";
let record = Record();

println(record[the_key]); // out: value

record[the_key] = "new value";

println(record[the_key]); // out: new value
