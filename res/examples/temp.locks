class Greeter {
  init (greeting) {
    this.greeting = greeting;
  }

  hello(name) {
    print "Hello" + " " + name;
  }
}

var name = "World";
var greeter = Greeter(name);

greeter.hello(); // out: Hello World
