class Base {
  foo() {
    print "Base.foo()";
  }
}

class Derived extends Base {
  bar() {
    print "Derived.bar()";
    super.foo();
  }
}

Derived().bar();
// out: Derived.bar()
// out: Base.foo()
