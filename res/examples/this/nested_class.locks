class Outer {
  fn method() {
    print(this); // out: <object Outer>

    fn f() {
      print(this); // out: <object Outer>

      class Inner {
        fn method() {
          print(this); // out: <object Inner>
        }
      }

      Inner().method();
    }
    f();
  }
}

Outer().method();
